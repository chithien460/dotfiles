- defaults:
   link:
      relink: true

- clean: ['~']

- link:
        #~/.tmux.conf: tmux.conf
        #~/.vim: vim
        ~/.config/nvim/init.vim: init.vim 

- create:
    #- ~/downloads
    #   - ~/.vim/undo-history

- shell:
        - [git submodule update --init --recursive, Installing submodules]
